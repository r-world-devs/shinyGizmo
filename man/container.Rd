% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/container.R
\name{container}
\alias{container}
\title{Create a CSS container query wrapper}
\usage{
container(
  name,
  ...,
  conditions = list(),
  type = "inline-size",
  class = NULL,
  .style = NULL
)
}
\arguments{
\item{name}{Container name. Used as the CSS \code{container-name} and
as a class on the wrapper element for CSS targeting.}

\item{...}{Child UI elements.}

\item{conditions}{List of \code{\link{condition}} objects defining the
CSS rules and container queries.}

\item{type}{Container type: \code{"inline-size"} (default), \code{"size"},
or \code{"normal"}.}

\item{class}{Additional CSS classes for the container div.}

\item{.style}{Additional inline CSS styles (as a string or
\code{\link[htmltools]{css}} output).}
}
\value{
A \code{\link[htmltools]{tag}} object.
}
\description{
Wraps child elements in a container with CSS Container Query support.
Allows responsive styles based on the container's own dimensions rather
than the viewport.
}
\examples{
container(
  name = "demo",
  grid_item(shiny::div(class = "a", "A"), area = "a"),
  grid_item(shiny::div(class = "b", "B"), area = "b"),
  conditions = list(
    condition(!!!grid(template_areas = list(c("a", "b")))),
    condition(
      query = "width < 600px",
      !!!grid(template_areas = list(c("a"), c("b")))
    )
  )
)

}
